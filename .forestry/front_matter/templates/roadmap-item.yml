---
label: Roadmap Item
hide_body: true
fields:
- name: status
  type: select
  default: []
  config:
    required: true
    options:
    - completed
    - in-progress
    - next-up
    source:
      type: simple
      section: 
      file: 
      path: 
  label: Status
- name: title
  type: text
  config:
    required: false
  label: Title
- name: content
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: true
    schema:
      format: markdown
  label: Content
- name: ctas
  type: blocks
  label: CTAs
  template_types:
  - cta
  config:
    min: 
    max: 
